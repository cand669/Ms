<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ein">

    <application
        android:allowBackup="true"
        android:name=".MyApp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".activity.SivaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CyActivity"/>
        <activity android:name=".activity.PxActivity"/>
        <activity android:name=".activity.PzActivity"/>
        <activity android:name=".activity.YdActivity"/>
    </application>

</manifest>